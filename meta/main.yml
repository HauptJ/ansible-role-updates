---
galaxy_info:
  author: Joshua Haupt
  description: Installs yum-cron and enables automatic updates
  company: hauptj.com
  license: Apache 2.0
  min_ansible_version: 2.3
  platforms:
  - name: EL
    versions:
    - 7
  galaxy_tags:
    - updates
    - security
dependencies:
  - role: geerlingguy.repo-epel
    when: updates_ci_test == true
  - role: geerlingguy.repo-remi
    when: updates_ci_test == true
